# Sirajganj Doctor — Step 13

Run now:
`npm.cmd run dev`

Database migrations:
01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 11b, 12

Step 13 adds the dedicated doctor directory, advanced filters, pagination,
shareable URLs, and approved public doctor profile routes.

Before starting, copy `.env.example` to `.env.local` and add the staging
Supabase URL and publishable key. Run
`supabase/12_public_doctor_profile_security.sql`, then follow
`docs/STEP13.md`.
