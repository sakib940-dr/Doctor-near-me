import { FormEvent, useEffect, useState } from 'react';
import { ArrowDown, ArrowUp, ImagePlus, LoaderCircle, Pencil, Plus, Trash2 } from 'lucide-react';
import { getImageUrl } from '../lib/storage';
import { providerGallery, providerInvestigationCosts, providerReviews, providerServices, providerSlider, providerTreatmentCosts, uploadProviderWebsiteImage, type ProviderCost, type ProviderGalleryImage, type ProviderReview, type ProviderService, type ProviderSliderImage } from '../services/providerWebsiteContent';

type Tab='services'|'gallery'|'slider'|'reviews'|'costs';
export default function ProviderWebsiteContentTabs({providerId}:{providerId:string}){const [tab,setTab]=useState<Tab>('services');return <div className="provider-web-admin"><nav className="provider-web-tabs">{(['services','gallery','slider','reviews','costs'] as Tab[]).map(t=><button type="button" className={tab===t?'active':''} onClick={()=>setTab(t)} key={t}>{t==='costs'?'Treatment / Investigation Costs':t[0].toUpperCase()+t.slice(1)}</button>)}</nav>{tab==='services'&&<Services providerId={providerId}/>} {tab==='gallery'&&<Gallery providerId={providerId}/>} {tab==='slider'&&<Slider providerId={providerId}/>} {tab==='reviews'&&<Reviews providerId={providerId}/>} {tab==='costs'&&<Costs providerId={providerId}/>}</div>}
const msg=(e:unknown)=>e instanceof Error?e.message:'কাজটি সম্পন্ন করা যায়নি';
function Controls({up,down,edit,del}:{up:()=>void;down:()=>void;edit:()=>void;del:()=>void}){return <div className="provider-row-controls"><button type="button" onClick={up}><ArrowUp/></button><button type="button" onClick={down}><ArrowDown/></button><button type="button" onClick={edit}><Pencil/></button><button type="button" onClick={del}><Trash2/></button></div>}
function Services({providerId}:{providerId:string}){const [rows,setRows]=useState<ProviderService[]>([]),[edit,setEdit]=useState<ProviderService|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null); const load=()=>providerServices.getAll(providerId).then(setRows).catch(e=>setError(msg(e))); useEffect(load,[providerId]); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{let image=edit?.image||null;const file=f.get('imageFile');if(file instanceof File&&file.size)image=await uploadProviderWebsiteImage(providerId,file,'service');const input={name:{bn:String(f.get('bn')||''),en:String(f.get('en')||'')},description:{bn:String(f.get('dbn')||''),en:String(f.get('den')||'')},icon:String(f.get('icon')||'')||null,image,is_active:f.get('active')==='on',sort_order:edit?.sort_order??rows.length};if(edit)await providerServices.update(providerId,edit.id,input);else await providerServices.create(providerId,input);setEdit(null);e.currentTarget.reset();await load();}catch(x){setError(msg(x))}finally{setBusy(false)}} async function move(i:number,d:number){const a=[...rows],j=i+d;if(j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];setRows(a);await providerServices.reorder(providerId,a.map(x=>x.id));}return <Panel title="Services"><ContentForm submit={submit} edit={edit} busy={busy} image/><div className="provider-admin-list">{rows.map((x,i)=><article key={x.id}>{x.image&&<img src={getImageUrl(x.image,'public-images')||x.image} alt=""/>}<div><h3>{x.name.bn||x.name.en}</h3><p>{x.description?.bn||x.description?.en}</p><small>{x.is_active?'Active':'Hidden'}</small></div><Controls up={()=>move(i,-1)} down={()=>move(i,1)} edit={()=>setEdit(x)} del={async()=>{await providerServices.remove(providerId,x.id);load()}}/></article>)}</div>{error&&<p className="auth-message error">{error}</p>}</Panel>}
function Gallery({providerId}:{providerId:string}){const [rows,setRows]=useState<ProviderGalleryImage[]>([]),[edit,setEdit]=useState<ProviderGalleryImage|null>(null);const load=()=>providerGallery.getAll(providerId).then(setRows);useEffect(load,[providerId]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);let image=edit?.image||null;const file=f.get('imageFile');if(file instanceof File&&file.size)image=await uploadProviderWebsiteImage(providerId,file,'gallery');const input={category_id:String(f.get('category')||'hospital'),caption:{bn:String(f.get('bn')||''),en:String(f.get('en')||'')},image,is_active:f.get('active')==='on',sort_order:edit?.sort_order??rows.length};if(edit)await providerGallery.update(providerId,edit.id,input);else await providerGallery.create(providerId,input);setEdit(null);e.currentTarget.reset();load()}async function move(i:number,d:number){const a=[...rows],j=i+d;if(j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];setRows(a);await providerGallery.reorder(providerId,a.map(x=>x.id))}return <Panel title="Gallery"><ContentForm submit={submit} edit={edit} image category/><div className="provider-admin-list gallery">{rows.map((x,i)=><article key={x.id}>{x.image&&<img src={getImageUrl(x.image,'public-images')||x.image} alt=""/>}<div><h3>{x.caption.bn||x.caption.en||'Gallery image'}</h3><small>{x.category_id} • {x.is_active?'Active':'Hidden'}</small></div><Controls up={()=>move(i,-1)} down={()=>move(i,1)} edit={()=>setEdit(x)} del={async()=>{await providerGallery.remove(providerId,x.id);load()}}/></article>)}</div></Panel>}
function Slider({providerId}:{providerId:string}){const [rows,setRows]=useState<ProviderSliderImage[]>([]),[edit,setEdit]=useState<ProviderSliderImage|null>(null);const load=()=>providerSlider.getAll(providerId).then(setRows);useEffect(load,[providerId]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);let image=edit?.image||null;const file=f.get('imageFile');if(file instanceof File&&file.size)image=await uploadProviderWebsiteImage(providerId,file,'slider');const input={caption:{bn:String(f.get('bn')||''),en:String(f.get('en')||'')},icon:String(f.get('icon')||'')||null,image,is_active:f.get('active')==='on',sort_order:edit?.sort_order??rows.length};if(edit)await providerSlider.update(providerId,edit.id,input);else await providerSlider.create(providerId,input);setEdit(null);e.currentTarget.reset();load()}async function move(i:number,d:number){const a=[...rows],j=i+d;if(j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];setRows(a);await providerSlider.reorder(providerId,a.map(x=>x.id))}return <Panel title="Homepage Slider"><ContentForm submit={submit} edit={edit} image/><div className="provider-admin-list gallery">{rows.map((x,i)=><article key={x.id}>{x.image&&<img src={getImageUrl(x.image,'public-images')||x.image} alt=""/>}<div><h3>{x.caption.bn||x.caption.en||'Slide'}</h3><small>{x.is_active?'Active':'Hidden'}</small></div><Controls up={()=>move(i,-1)} down={()=>move(i,1)} edit={()=>setEdit(x)} del={async()=>{await providerSlider.remove(providerId,x.id);load()}}/></article>)}</div></Panel>}
function Reviews({providerId}:{providerId:string}){const [rows,setRows]=useState<ProviderReview[]>([]),[edit,setEdit]=useState<ProviderReview|null>(null);const load=()=>providerReviews.getAll(providerId).then(setRows);useEffect(load,[providerId]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const input={name:String(f.get('name')||''),rating:Number(f.get('rating')||5),comment:String(f.get('comment')||''),reply:{bn:String(f.get('reply')||''),en:''},replied_at:f.get('reply')?new Date().toISOString():null,is_published:f.get('active')==='on',sort_order:edit?.sort_order??rows.length};if(edit)await providerReviews.update(providerId,edit.id,input);else await providerReviews.create(providerId,input);setEdit(null);e.currentTarget.reset();load()}async function move(i:number,d:number){const a=[...rows],j=i+d;if(j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];setRows(a);await providerReviews.reorder(providerId,a.map(x=>x.id))}return <Panel title="Reviews"><form className="provider-content-form" onSubmit={submit}><input name="name" defaultValue={edit?.name||''} required placeholder="Reviewer name"/><input name="rating" type="number" min="1" max="5" defaultValue={edit?.rating||5}/><textarea name="comment" defaultValue={edit?.comment||''} placeholder="Review"/><textarea name="reply" defaultValue={edit?.reply?.bn||''} placeholder="Provider reply"/><label><input name="active" type="checkbox" defaultChecked={edit?.is_published??true}/> Published</label><button><Plus/> Save review</button></form><div className="provider-admin-list">{rows.map((x,i)=><article key={x.id}><div><h3>{x.name} • {x.rating}/5</h3><p>{x.comment||x.text?.bn}</p><small>{x.is_published?'Published':'Pending/hidden'}</small></div><Controls up={()=>move(i,-1)} down={()=>move(i,1)} edit={()=>setEdit(x)} del={async()=>{await providerReviews.remove(providerId,x.id);load()}}/></article>)}</div></Panel>}
function Costs({providerId}:{providerId:string}){return <div className="provider-cost-admin-grid"><CostEditor providerId={providerId} title="Treatment Costs" api={providerTreatmentCosts}/><CostEditor providerId={providerId} title="Investigation Costs" api={providerInvestigationCosts}/></div>}
function CostEditor({providerId,title,api}:{providerId:string;title:string;api:typeof providerTreatmentCosts}){const [rows,setRows]=useState<ProviderCost[]>([]),[edit,setEdit]=useState<ProviderCost|null>(null);const load=()=>api.getAll(providerId).then(setRows);useEffect(load,[providerId]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),input={name:{bn:String(f.get('bn')||''),en:String(f.get('en')||'')},cost:{bn:String(f.get('costbn')||''),en:String(f.get('costen')||'')},sort_order:edit?.sort_order??rows.length};if(edit)await api.update(providerId,edit.id,input);else await api.create(providerId,input);setEdit(null);e.currentTarget.reset();load()}async function move(i:number,d:number){const a=[...rows],j=i+d;if(j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];setRows(a);await api.reorder(providerId,a.map(x=>x.id))}return <Panel title={title}><form className="provider-content-form" onSubmit={submit}><input name="bn" defaultValue={edit?.name.bn||''} required placeholder="বাংলা নাম"/><input name="en" defaultValue={edit?.name.en||''} placeholder="English name"/><input name="costbn" defaultValue={edit?.cost.bn||''} required placeholder="খরচ"/><input name="costen" defaultValue={edit?.cost.en||''} placeholder="Cost English"/><button><Plus/> Save</button></form><div className="provider-admin-list">{rows.map((x,i)=><article key={x.id}><div><h3>{x.name.bn||x.name.en}</h3><p>{x.cost.bn||x.cost.en}</p></div><Controls up={()=>move(i,-1)} down={()=>move(i,1)} edit={()=>setEdit(x)} del={async()=>{await api.remove(providerId,x.id);load()}}/></article>)}</div></Panel>}
function Panel({title,children}:{title:string;children:React.ReactNode}){return <section className="provider-form-section provider-content-panel"><h2>{title}</h2>{children}</section>}
function ContentForm({submit,edit,busy=false,image=false,category=false}:{submit:(e:FormEvent<HTMLFormElement>)=>void;edit:any;busy?:boolean;image?:boolean;category?:boolean}){return <form className="provider-content-form" onSubmit={submit}><input name="bn" defaultValue={edit?.name?.bn||edit?.caption?.bn||''} required placeholder="বাংলা নাম / caption"/><input name="en" defaultValue={edit?.name?.en||edit?.caption?.en||''} placeholder="English name / caption"/>{edit?.description!==undefined&&<><textarea name="dbn" defaultValue={edit?.description?.bn||''} placeholder="বাংলা description"/><textarea name="den" defaultValue={edit?.description?.en||''} placeholder="English description"/></>}<input name="icon" defaultValue={edit?.icon||''} placeholder="Icon name (optional)"/>{category&&<select name="category" defaultValue={edit?.category_id||'hospital'}><option value="hospital">Hospital</option><option value="doctors">Doctors</option><option value="staff">Staff</option><option value="ot">Operation Theatre</option><option value="success">Success</option></select>}{image&&<label className="provider-file"><ImagePlus/> Upload image<input name="imageFile" type="file" accept="image/jpeg,image/png,image/webp,image/avif"/></label>}<label><input name="active" type="checkbox" defaultChecked={edit?.is_active??true}/> Active</label><button disabled={busy}>{busy?<LoaderCircle className="spin"/>:<Plus/>} Save</button></form>}
