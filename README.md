# Sirajganj Doctor — Step 12

Run now:
`npm.cmd run dev`

Database migrations:
01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 11b

Step 12 adds the React/Vite/TypeScript/Tailwind public frontend and connects
the homepage, doctor search, and ambulance search to the public Supabase RPCs.

Before starting, copy `.env.example` to `.env.local` and add the staging
Supabase URL and publishable key. Full instructions are in `docs/STEP12.md`.
